using JeuClass;
using System.Runtime.CompilerServices;

namespace TestUnitaires
{
    [TestClass]
    public class UnitTest_Des
    {
       /* [TestMethod]
        public void Constructeur_1_10_RetourInstanceOKEtControl()
        {
            De de = new De(1, 10);
            Assert.IsTrue(De.valeurMax == 10, "Probleme intitialisation valeure maximale");
            Assert.IsTrue(De.valeurMin == 1, "Probleme initialisation valeure minimale");
        }

        public void Rouler_DesMib1Max6_ValeurCompriseEntre1Et6()
        {
            De de = new De(1, 6);
            de.Rouler();
            for (int i = 0; i < 100; i++)
                Assert.IsTrue(de.GetValeur() >= 1 && de.GetValeur() <= 6, "Probleme valeure n'est pas dans la bonne plage");
        }*/
    }
}